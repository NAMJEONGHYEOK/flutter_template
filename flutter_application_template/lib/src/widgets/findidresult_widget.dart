//StatelessWidget 으로 작성

import 'package:flutter/material.dart';

class FindIdResultWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("아이디 결과 화면"),
    );
  }
}
