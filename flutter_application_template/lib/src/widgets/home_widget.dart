import 'package:flutter/material.dart';

class Home_widget extends StatelessWidget {
  const Home_widget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Flutter provider pattern"),
    );
  }
}
